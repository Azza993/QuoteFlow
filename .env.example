# QuoteFlow runs on realistic demo data out of the box — no configuration needed.
# Set both of these to point the app at a real Supabase project instead.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: force a backend regardless of the variables above ("demo" | "supabase")
VITE_DATA_BACKEND=
